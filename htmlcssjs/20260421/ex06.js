const id = "아이디";
const pw = "비밀번호";

const person = {
    idd,
    pw: pw,
    name: "홍길동",
    age: 30,
    phone: "010-1234-5678",
    찬열: '시발련'
    

}
console.log(person);
for (const key in person) {
    
    const element = person[key];
    console.log(`key ${key} element ${element}`);
    
}