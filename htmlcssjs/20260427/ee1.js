const user = {
    id: '아이디',
    pw: '비밀번호',
    name: '이름',
    age: '나이',
}
const id = user.id;
const pw = user.pw;
const name = user.name;
const age = user.age;

console.log(id);
console.log(pw);
console.log(name);
console.log(age);
